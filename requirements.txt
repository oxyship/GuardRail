# Core — no dependencies needed for the regex-based baseline

# Run the demo with just:
#   pip install -r requirements.txt
#   python demo.py

pytest>=7.0

# ─── Optional upgrades (uncomment to use) ─────────────────────────────────────

# PII detection (production-grade, replaces regex layer)
# presidio-analyzer>=2.2
# presidio-anonymizer>=2.2
# spacy>=3.0
# en-core-web-lg  # download with: python -m spacy download en_core_web_lg

# Toxicity classification (ML-based, replaces keyword layer)
# detoxify>=0.5

# Topic drift via semantic similarity (replaces keyword overlap)
# sentence-transformers>=2.2

# Wrap as a REST API
# fastapi>=0.100
# uvicorn>=0.22
